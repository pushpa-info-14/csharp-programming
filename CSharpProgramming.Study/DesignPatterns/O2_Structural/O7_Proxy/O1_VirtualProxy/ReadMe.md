## Virtual Proxy

### When
There’s a need to delay the creation and initialization of resource-intensive objects until they’re needed.

### Purpose: 
Acts as a placeholder for the expensive object and initializes it on demand, optimizing performance (lazy initialization).